type Transaction @model(subscriptions: { level: public }) @auth(rules: [{allow: owner, operations: [read, update, create]}]) {
  id: ID!
  amount: Float
  from: String
  to: String
  owner: String
}
 